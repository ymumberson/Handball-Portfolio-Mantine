# # File: .github/workflows/publish.yml
# name: publish-to-github-pages
# on:
#   push:
#     branches:
#       - master

# permissions:
#   contents: read
#   pages: write
#   id-token: write

# concurrency:
#   group: "pages"
#   cancel-in-progress: false

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout 🛎️
#         uses: actions/checkout@v4

#       - name: Setup Node.js ⚙️ - Cache dependencies ⚡ - Install dependencies 🔧
#         uses: ./.github/workflows/setup-node
      
#       # - name: Double installing dependencies
#       #   uses: npm i

#       - name: Setup Pages ⚙️
#         uses: actions/configure-pages@v4
#         with:
#           static_site_generator: next

#       - name: Build with Next.js 🏗️
#         run: npx --no-install next build

#       - name: List contents of the current directory 📁
#         run: ls -la
      
#       - name: List contents of git ignore
#         run: cat .gitignore

#       - name: Upload artifact 📡
#         uses: actions/upload-pages-artifact@v3
#         with:
#           path: ./out

#   deploy:
#     environment:
#       name: github-pages
#       url: ${{ steps.deployment.outputs.page_url }}

#     runs-on: ubuntu-latest
#     needs: build

#     steps:
#       - name: Publish to GitHub Pages 🚀
#         id: deployment
#         uses: actions/deploy-pages@v4

name: Deploy to GitHub Pages

on:
  push:
    branches:
      - master # Adjust if your default branch is different
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20' # Ensure compatibility with your local version
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --immutable

      - name: Build the project
        run: yarn build

      - name: Export static pages
        run: yarn next export

      - name: Upload artifact to GitHub Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./out # Make sure this matches the output folder from `next export`

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4